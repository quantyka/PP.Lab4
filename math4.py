def parallelogram_area(base, height):
    return base * height

base = float(input("Enter the base: "))
height = float(input("Enter the height: "))

print("Area of the parallelogram:", parallelogram_area(base, height))